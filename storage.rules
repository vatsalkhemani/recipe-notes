rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Audio + cover photos live under recipes/{uid}/... and are private to the user.
    match /recipes/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
